all:
 hosts:
  localhost:
 children:
  servers:
   hosts:
    localhost:
  switches:
   hosts:
    veos-pol-06.localdomain
    n9k-standalone-06.localdomain
  routers:
   hosts:
   csr1000v-pod-06.localdomain
